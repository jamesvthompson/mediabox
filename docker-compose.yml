# Compatibility shim. Run ./mediabox.sh (optionally with --apps) to generate docker-compose.generated.yml
services:
  _shim:
    image: alpine
    command: ["sh", "-c", "echo 'Run ./mediabox.sh to generate and launch Mediabox.' && sleep 3600"]
